ROADMAP:
========

1. encode canonical table in standard format
2. encode to binary sequence
3. try (en/de)coding the README (this file).
4. make it DEFLATE compatible, so it can be used in zip, pdf, etc...
